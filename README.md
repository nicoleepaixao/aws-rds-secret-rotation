# aws-rds-secret-rotation
Automated credential rotation for Amazon RDS using AWS Secrets Manager and a custom Lambda rotation function. Includes full rotation workflow (createSecret, setSecret, testSecret, finishSecret), secure IAM practices, VPC configuration and production-ready examples.
